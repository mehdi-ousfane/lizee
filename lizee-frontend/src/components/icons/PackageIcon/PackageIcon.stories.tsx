import { Meta } from '@storybook/react/types-6-0';
import PackageIcon from './PackageIcon';

export default {
  title: 'icons/PackageIcon',
  component: PackageIcon,
} as Meta;

export const Primary = () => <PackageIcon />;
